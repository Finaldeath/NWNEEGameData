int StartingConditional()
{
    if(GetLocalInt(GetModule(),"vejoni_plot")>9 &&
       GetLocalInt(GetModule(),"vejoni_plot")<25)
    return TRUE;
    return FALSE;
    //Lucas has implicated Vejoni in the theft but
    //the story is as yet unconfirmed.
}
