int StartingConditional()
{
    if(GetLocalInt(GetModule(), "lashar_has_introduced")==1)

    return TRUE;
    return FALSE;

    //lashar has pointed the player towards White Glove's store and now
    //returns to his standard shop routine
}
